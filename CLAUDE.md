# AetherLink — Core Directives

- This is a TypeScript monorepo using **pnpm workspaces**
- Bot is in `apps/waBot`, webapp in `apps/webapp`
- Supabase client is already configured — do not reinit
- Do not touch `.env` files — only modify `.env.example`
- Always run `pnpm typecheck` after making edits