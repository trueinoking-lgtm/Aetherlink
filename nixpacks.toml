[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install -g pnpm", "pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @aetherlink/webapp build"]

[start]
cmd = "pnpm --filter @aetherlink/webapp start"
